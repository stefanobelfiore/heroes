import { EnvironmentModel } from "./models/environment.models";

export const environment: EnvironmentModel = {
  apiDefault: '',//url según entorno, los environments pueden ser diferentes, mirar los cambios en el angular.json.
  production: false,
};